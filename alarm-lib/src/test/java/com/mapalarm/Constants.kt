package com.mapalarm

import com.mapalarm.datatypes.Position

class Constants {
    companion object {
        val defaultLat = 0.0
        val defaultLng = 0.0
        val defaultPosition = Position(defaultLat, defaultLng)
        val defaultRadius = 200.0
    }
}
